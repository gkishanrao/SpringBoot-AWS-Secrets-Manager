aws:
  region: us-east-1
  secretsmanager:
    secret-name: prod/aws/credentials

application:
  bucket:
    name: us-east-prod

spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 9090
